<UserControl x:Class="PereMaria.GestorHotel.Views.RoomMaintenanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PereMaria.GestorHotel.Views"
                xmlns:controllers="clr-namespace:PereMaria.GestorHotel.Controllers"
                DataContext="{x:Static controllers:RoomsViewModel.Instance}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.Resources>
            <!-- Style -->
            <Style TargetType="Label">
                <Setter Property="Foreground" Value="{StaticResource Bg-Light}"/>
                <Setter Property="FontSize" Value="20px"/>
            </Style>
        </Grid.Resources>
        <StackPanel Grid.Row="0" Margin="10">
            <Label Content="Tiempo de limpieza" />
            <TextBox Width="auto" Style="{StaticResource TextBoxStyle}" Text="{Binding CurrentRoom.CleaningTime}" PreviewTextInput="Int_PreviewTextInput" MaxLength="2"/>
            <Button Content="Guardar" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SaveCtCommand}" Margin="10" HorizontalAlignment="Right"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="10">

            <!-- Recuadro Mantenimiento -->
            <Border BorderBrush="{StaticResource Bg-Purple-Accent}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,15">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CurrentRoom.Maintenance}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel>
                    <TextBlock Text="¡HABITACIÓN EN MANTENIMIENTO ACTUALMENTE!" Foreground="#FF4C6A" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Motivo: " Foreground="{StaticResource Bg-Cosmic-Light}"/>
                        <TextBlock Text="{Binding CurrentRoom.MaintenanceReason}" Foreground="{StaticResource Bg-Light}" FontWeight="SemiBold" Margin="0,0,20,0"/>
                        <TextBlock Text="Hasta: " Foreground="{StaticResource Bg-Cosmic-Light}"/>
                        <TextBlock Text="{Binding CurrentRoom.MaintenanceTime}" Foreground="{StaticResource Bg-Light}" FontWeight="SemiBold"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Label Content="Poner habitación en mantenimiento" />
            <StackPanel Orientation="Horizontal">
                <Label Content="Fecha:" Style="{StaticResource FormLabelStyle}" VerticalAlignment="Center" Margin="5"/>
                <DatePicker Style="{StaticResource DatePickerStyle}" SelectedDateFormat="Short" Width="150">
                    <DatePicker.SelectedDate>
                        <Binding Path="DateText" Mode="TwoWay" Converter="{StaticResource DateStringToDateTime}" />
                    </DatePicker.SelectedDate>
                </DatePicker>
                <Label Content="Hora: " Style="{StaticResource FormLabelStyle}" VerticalAlignment="Center" Margin="5"/>
                <TextBox Width="70" Style="{StaticResource TextBoxStyle}" Text="{Binding MaintenanceHour, UpdateSourceTrigger=PropertyChanged}" PreviewTextInput="Int_PreviewTextInput" MaxLength="2"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Label Content="Motivo:" Style="{StaticResource FormLabelStyle}" VerticalAlignment="Center" Margin="5"/>

                <ComboBox x:Name="ReasonCombo" Style="{StaticResource DropdownStyle}" Width="200" Margin="10" ItemsSource="{Binding MaintenanceReasons}" 
              SelectedItem="{Binding SelectReason, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>

                <TextBox HorizontalAlignment="Left" Width="200" Margin="10" Text="{Binding CustomReason, UpdateSourceTrigger=PropertyChanged}">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource TextBoxStyle}">
                            <Setter Property="IsEnabled" Value="False"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=ReasonCombo, Path=SelectedItem}" Value="Otro">
                                    <Setter Property="IsEnabled" Value="True"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10" HorizontalAlignment="Right">

                <Button Content="Cancelar Mantenimiento" Command="{Binding QuitMaintenanceCommand}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
                            <Setter Property="IsEnabled" Value="False"/>
                            <Setter Property="Opacity" Value="0.7"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding CurrentRoom.Maintenance}" Value="True">
                                    <Setter Property="IsEnabled" Value="True"/>
                                    <Setter Property="Opacity" Value="1"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="Poner en mantenimiento" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding SetMaintCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
