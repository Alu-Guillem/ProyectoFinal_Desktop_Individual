<UserControl x:Class="PereMaria.GestorHotel.Components.AvatarInitial"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="40" d:DesignWidth="40">

    <Grid x:Name="RootGrid" Width="40" Height="40">
        <!-- Fondo con color aleatorio -->
        <Ellipse x:Name="BackgroundEllipse" Fill="#6A4C93"/>

        <!-- Inner shadow - simula gradiente de claro (centro) a oscuro (bordes) -->
        <Ellipse>
            <Ellipse.Fill>
                <RadialGradientBrush GradientOrigin="0.3,0.3" Center="0.5,0.5" RadiusX="0.6" RadiusY="0.6">
                    <GradientStop Color="#20FFFFFF" Offset="0"/>
                    <GradientStop Color="Transparent" Offset="0.4"/>
                    <GradientStop Color="#30000000" Offset="0.75"/>
                    <GradientStop Color="#60000000" Offset="1"/>
                </RadialGradientBrush>
            </Ellipse.Fill>
        </Ellipse>

        <!-- Letra centrada -->
        <TextBlock x:Name="InitialText"
                   Text="A"
                   FontSize="18"
                   FontWeight="Bold"
                   Foreground="{StaticResource Text-Primary}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"/>
    </Grid>
</UserControl>
